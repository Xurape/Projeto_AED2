void carregarDados() {
  // FILE *f_utentes = NULL, *f_medicos = NULL;

  // if(!(f_utentes = fopen("database/utentes.txt", "a+"))) {
  //   printf("Erro ao abrir ficheiro dos utentes. A sair..");
  //   exit(1);
  // }

  // if(!(f_medicos = fopen("database/medicos.txt", "a+"))) {
  //   printf("Erro ao abrir ficheiro dos medicos. A sair..");
  //   exit(1);
  // }

  // while(!feof(f_medicos)) {
  //   fscanf(f_medicos, "%d,%d,%[^\n]s", &medico[medicos].codigo, medico[medicos].filaEspera, medico[medicos].nome);
  //   realocarEstruturas();
  //   medicos++;
  // }

  // while(!feof(f_utentes)) {
  //   fscanf(f_utentes, "%d,%d", &utente[utentes].codigo, &utente[utentes].medicoFamilia);
  //   realocarEstruturas();
  //   utentes++;
  // }
}
